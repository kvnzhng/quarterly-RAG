# Notes -- quarterly-RAG

> Claude's scratch pad for cross-session observations. Updated by `/project-update`.

## Observations

- 2026-09-03: Ollama is not installed yet; Docker is installed but was not running at init. Both are needed from RAG-002 / RAG-013 onward.
- 2026-09-04: Models run on Kevin's network Ollama server (v0.32.13; address in `.env` only). It holds an 8B to 32B ladder (llama3.1:8b, gpt-oss:20b, mistral-small, gemma4:26b, qwen3.6:27b, qwen3.8-27b, deepseek-r1:32b), so local comparisons are not laptop-bound. There is no Ollama on the laptop; `make models` pulls over the HTTP API. Warm `rag doctor`: chat 242 ms, embedding 38 ms.
- 2026-09-03: `jq` on PATH comes from Anaconda (`/opt/homebrew/anaconda3/bin/jq`); the ticket hook depends on it.

- 2026-09-03: Kevin runs a local AI server on the network; its address is given at RAG-002 start and lives in `.env` only. Provider choice is the user's (ADR-005).
- 2026-09-04: Eval set v0 (RAG-019) is 43 human-verified questions over 6 filings. Deliberate near-misses the corpus does *not* answer: iPhone unit sales (Apple stopped disclosing in 2018), Vision Pro revenue (inside Wearables), per-country headcount, executive compensation (proxy, incorporated by reference). Nvidia's segment revenue ($193,479M Compute & Networking) and Data Center end-market revenue ($193,737M) are close but different cuts, which is a good retrieval trap.
- 2026-09-04: Phase two closed. Findings in order of discovery: nomic prefixes were missing and cost a third of recall (RAG-006); the 8B model invents citations in half its answers and citation discipline is a model capability (RAG-010); the retrieval-score threshold is worse than useless as a refusal signal on this corpus (RAG-011); fusion beats both of its parts (RAG-009); the ticker filter buys nothing and the quarter filter fixes the 10-Q questions (RAG-026); chunking on the filing's own sub-headings nearly doubles recall@1 (RAG-020); FAISS is 16x faster and it is 3% of a retrieval (RAG-007); RAGAS is anti-correlated with local models (RAG-012). Remaining ceiling: recall@20 is 72.7%.
- 2026-09-04: Process failures this session: two commits whose messages described doc edits that had not applied, one ticket citing a discarded hash, one conclusion generalised beyond its experiment, one model measured at an unfair token budget. All caught and corrected; the rules are now in `project/conventions.md`.
- 2026-09-04: Corpus v0 on disk: 8 filings per company for the two-year default window (AAPL FY2024 10-K to FY2026 Q3; NVDA FY2025 Q3 to FY2027 Q2), 19 MB of inline-XBRL HTML, 0.7 to 2.1 MB each. EDGAR's `size` field is the whole submission, not the primary document. Two-year default `--since` drifts with the calendar; RAG-019 pins accession numbers.
- 2026-09-04: External review of the scaffold. Accepted: ship one thin end-to-end path before comparing tools; build the eval set right after parsing; label evidence as spans, not chunk ids; flag derived numbers and add calculation provenance; attach a run record to every number; make ticket enforcement portable; fix README tense and stale lines. Backlog reordered into phases, RAG-019 to RAG-023 added.

## Open Questions

- Apple and Nvidia are the starting tickers. Add a third company with a different fiscal calendar (e.g. Microsoft, June FY) to stress period filtering? Now more interesting: the quarter filter (RAG-026) and the scope check (RAG-011) both key on company words, and the near-miss ladder says filing-level retrieval is the solved part.
- The eval set concentrates its evidence in 6 of 16 filings and is 70% tables. Both were noted at RAG-019 and neither has been fixed. A second labelling round spread across quarters would sharpen every retrieval number.
- Recall@20 is 72.7%: roughly a quarter of questions have evidence neither ranking nor chunking reaches. Nobody has looked at which questions those are since RAG-020 changed the chunks.
- The regression gate scores the 23 `lookup` questions only. Calculation provenance (RAG-021) is measured but ungated, so nothing fails if a change breaks it. Adding `derived` and `cross_period` to `rag eval baseline` would fix that, at the cost of a longer gate.
- `rag eval refusal` and the refusal eval inside `rag eval baseline` disagree on the same measurement, and not by a little: they disagree on the *direction*. Standalone gave 63.6% answerable coverage under prompt v1 and 66.7% under v2, so v2 refused less; the gate gave 66.7% under v1 and 60.6% under v2, so v2 refused more. Each is reproducible in its own harness, and the two commands build the pipeline from the same settings. The generation eval, by contrast, agrees between the harnesses to the question: `generation-retrieved-20260904T194220` and the gate's v2 row both report 13 of 15 fully grounded, 15 of 15 correct and 0.733 faithfulness. So whatever differs is in the refusal path alone. Found at RAG-021 while deciding whether a gate failure was real; the gate's numbers are the ones quoted, because they are the ones the baseline was recorded with. Update 2026-09-05 (RAG-015): the gate itself gave 63.6% under v1 that day, the standalone's number; the disagreement is one question wide, the size of the cross-day drift below, so a harness difference is now the less likely explanation.
- ~~The gate is deterministic where it was assumed noisy: run twice under prompt v2 it gave identical numbers to three decimals, and under v1 it reproduced all nine committed metrics exactly. A metric that moves is a change, not a wobble.~~ Corrected 2026-09-05 (RAG-015): the gate is deterministic *within a day* and not across days. Two runs on 2026-09-05 at `3daa950` agreed with each other to three decimals and disagreed with the 2026-09-04 baseline on the three model-dependent metrics: answerable coverage 0.667 to 0.636, abstention F1 0.812 to 0.800, faithfulness 0.750 to 0.625, with identical code, questions, model digests and server version. `gpt-oss:20b` at temperature 0 gave byte-identical answers on 13 of 23 lookup questions across the two days, and the judge flipped two verdicts on sentences whose only change was spacing; judge looser stayed 0. Server-side state, most likely how the generator sat in memory next to the 27B judge, is the candidate cause, unverified. A metric that moves by one question is a change or a day, and only a per-question comparison says which. The gate writes none, which is the first thing to fix.
- 2026-09-05: MRR depends on the retrieval depth, because it is computed over the retrieved list. The chunking comparison (RAG-020) ran at depth 20 and reports section-aware at 0.449; the gate runs at depth 10 and reports 0.440. Same ranking, different cut; quote the depth with the number.
- 2026-09-05: the course exists: twelve Notion chapters as a private draft in Kevin's workspace (parent URL in the README) and `notebooks/course.py` (RAG-034). Kevin decides whether to share the pages to the web or move them under his Private-AI Engineer Course.
- Langfuse v4 defaults to `events_only` mode, where the legacy `/api/public/traces` endpoint is gone and the SDK's own `trace.get` returns 404. Traces are read through `/api/public/v2/observations` and the metrics API instead. The list endpoint does not project `model`, `usageDetails`, `input`, `output` or `metadata`, so an observation can look empty while holding them: the metrics API reported 1,149 tokens and the model name on a generation span the list endpoint showed as `usage={}` and `model=None`. Input, output and metadata are inferred to be present by the same argument and have not been read back. Anything written against the old API needs re-checking after a Langfuse upgrade. Scores are on `/api/public/v3/scores`, a third version again, and they carry `environment: default` while the spans carry `local`, so the two do not agree on a field they both have.
- Calculation provenance is off by default because the gate said so, not because it does not work. Prompt v2 costs two of the 33 answerable questions. Whether a prompt that separates the two rules better can keep the arithmetic without the refusals is untested.
- Prompt wording moved the RAG-021 numbers more than the rule did. Two v2 wordings of the same rule differ only in where the worked example sits: with it last, `llama3.1:8b` verified 9 of 13 calculations and `gpt-oss:20b` lost 11 points of lookup faithfulness; with it in the body, lookup faithfulness came back and the 8B model verified 6 of 10. A worked example is a style anchor for every model in the prompt, not just for the behaviour it demonstrates.
- ~~A question naming two companies retrieves only one of them.~~ Fixed in RAG-031 by asking each named company separately and interleaving by rank: every wording now returns 3 and 3 where the worst returned 6 Nvidia and 0 Apple. The comparison question is still refused, for a different reason.
- Retrieval is unstable to phrasing and only for Nvidia: adding the word "total" moves its income statement from rank 2 to outside the top 6, while the same edits leave Apple at rank 1. Term frequency inside one company's filings is the mechanism, and it is a candidate explanation for the recall@20 ceiling of 72.7% that has never been explained. RAG-032, with the numbers in `docs/learning/retrieval-quality.md`.
- `q052` (which customers account for Nvidia's largest sales) leaks past every model's refusal. Is that a prompt problem or a genuinely unanswerable-looking-answerable question?
- ~~Should a sample of processed filings be committed for CI tests?~~ Resolved 2026-09-04 (RAG-004): no. Two truncated HTML fixtures of about 2 KB each under `tests/ingestion/fixtures/` cover the parser's rules; the real 16 filings are exercised by an integration test that skips when the corpus is absent. Eval sets are still committed under `data/eval/`.
